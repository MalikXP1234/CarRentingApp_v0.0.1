{% extends "partials/base2.jinja" %}
{% block content %}

<!-- Aadam Malik -->

<div class="position-absolute top-50 start-50 translate-middle">
    <div class="border border-black container mt-5">

        <h1 class="text-center mb-8">Forgotten Password?</h1>

        <h4 class="text-center mb-8"> We will send you a reset password :)</h4>

        <!-- Gives a input box for the user to submit and send the email -->
        <div class="mb-3">
            <label for="email" class="form-label">Email Address</label>
            <input type="email" class="form-control" id="email" placeholder="Enter your email" required>
        </div>
        <div class="d-flex justify-content-between"><a href="Login.html" class="btn btn-primary">Send Link</a>
        </div>
    </div>
</div>

    {% endblock %}